<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.daka.mapper.UserMapper" >

  <insert id="insertUser" parameterType="com.daka.model.UserModel">
    insert into t_user (
            user_name,
            pass,
            mobile
        )values (
            #{userName},
            #{pass},
            #{mobile}
            )
  </insert>
</mapper>